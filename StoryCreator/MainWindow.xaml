<Window x:Class="StoryCreator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StoryCreator"
        mc:Ignorable="d" Icon="Images/AppIcon.png" Height="651" Width="829" Background="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}"
        >
    <Grid Background="{DynamicResource {x:Static SystemColors.GradientInactiveCaptionBrushKey}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="78*"/>
            <RowDefinition Height="545*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="workspaceGrid" Margin="10" Grid.Row="1" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="662*"/>
                <ColumnDefinition Width="141*"/>
            </Grid.ColumnDefinitions>
            <Canvas x:Name="workspaceCanvas" ClipToBounds="True" Margin="10" Background="GhostWhite"></Canvas>
        </Grid>

    </Grid>
</Window>
